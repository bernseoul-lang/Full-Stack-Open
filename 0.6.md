sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note into the text field and clicks Save

    Note right of browser: JavaScript handles the form submit event
    Note right of browser: JavaScript creates a new note object
    Note right of browser: JavaScript adds the new note to the notes list
    Note right of browser: Browser re-renders the note list immediately

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: Server saves the new note
    server-->>browser: {"message":"note created"}
    deactivate server