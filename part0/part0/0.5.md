sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note into the text field and clicks the Save button

    Note right of browser: JavaScript intercepts the form submit event
    Note right of browser: JavaScript creates a new note object and adds it to the notes list
    Note right of browser: Browser redraws the notes on the page immediately

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: Server saves the new note
    server-->>browser: {"message":"note created"}
    deactivate server
 